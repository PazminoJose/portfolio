---
import { cn } from "@/lib/utils";

interface Props {
  class?: string;
}
const { class: className } = Astro.props;
---

<div class="flex items-center gap-2">
  <span class={cn("h-3 w-3 rounded-full bg-gray-200", className)}> </span>
  <slot />
</div>
